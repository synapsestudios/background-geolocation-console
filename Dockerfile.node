FROM node:13.2

ARG PACKAGE_PATH=.
ARG WORKING_DIR=/src

WORKDIR $WORKING_DIR

COPY $PACKAGE_PATH/package*.json $WORKING_DIR/
RUN npm install

COPY $PACKAGE_PATH $WORKING_DIR
RUN npm run build

CMD [ "npm", "start" ]